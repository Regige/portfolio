import { ObserveDlementDirective } from './observe-dlement.directive';

describe('ObserveDlementDirective', () => {
  it('should create an instance', () => {
    const directive = new ObserveDlementDirective();
    expect(directive).toBeTruthy();
  });
});
